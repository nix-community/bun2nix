{
  "name": "@workspace/app",
  "version": "1.0.0",
  "scripts": {
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile dist/app ./index.js"
  },
  "dependencies": {
    "@workspace/lib": "workspace:*"
  }
}
